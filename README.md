# Formal_verification_of_Deep_Neural_Networks
- This repo contains `Colab` notebooks corresponding to this project `report`.
- For a few outputs, the percentage is not calculated in the notebook's output because of compute limitations, so I have calculated the percentage for them manually
